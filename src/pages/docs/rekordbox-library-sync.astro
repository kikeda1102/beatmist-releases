---
import Layout from "../../layouts/Layout.astro";
import rekordboxLibrarySync from "../../components/rekordboxLibrarySync";
import { site } from "../../data/content";
---

<Layout
  title={`${site.name} - rekordboxライブラリの自動更新`}
  description="BeatMistの設定でrekordboxライブラリ更新を有効にすると、ファイル変換やrekordboxプレイリスト・MyTagの操作がrekordboxデータベースに自動で反映されます。"
>
  <rekordboxLibrarySync client:only="react" />
</Layout>
